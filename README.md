go build it with cargo
> cargo build

and then inject the dll into the game and you should have it working (FOR THE LATEST VERSION OF GRANNY LEGACY IF YOU USE A DIFFERENT VERSION IT MIGHT OVERWRITE NOT THE EXPECTED BYTES)

you can use standard windows injection or manual map to inject it either way works